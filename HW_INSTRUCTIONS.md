# คู่มือและคำอธิบายสำหรับส่งการบ้าน (Homework Instructions & Explanations)

เอกสารนี้รวบรวมข้อมูลสำหรับส่งการบ้านวิชา AI for Robotic

## 1. การส่งงานสำหรับเย็นนี้ (ภาพแคปจอสนามกระดูกและตัวหนู)
- **วิธีการ**: ให้เปิดโปรแกรมจำลองขึ้นมาโดยรันคำสั่ง `python maze_simulation.py` ใน Terminal หรือ Command Prompt
- เมื่อหน้าต่างโปรแกรมเปิดขึ้นมา จะแสดงผล **2D Top-down View** ของเขาวงกตขนาด 30x30 ช่อง ที่สุ่มสร้างด้วย `random.seed(42)` เพื่อให้ได้แผนที่คงที่
- ตัวหนูจะอยู่ที่จุดเริ่มต้น (มุมซ้ายบน) และจะมองเห็นเส้นตรงชี้ไปยังก้อนชีส (มุมขวาล่าง)
- **ให้แคปเจอร์หน้าต่างโปรแกรมนี้ (Screenshot) เพื่อนำไปส่งในตอนเย็นได้ทันที**

## 2. อัลกอริทึมที่ใช้ (สำหรับส่งวันจันทร์หน้า)
- **กฎที่หนูใช้เดิน**: **Right-Hand Rule (กฎการเดินชิดกำแพงขวา)** ซึ่งเป็น Local Decision
- **คำอธิบาย**: หนูจะไม่มีการประมวลผลหรือคำนวณเส้นทางทั้งหมดล่วงหน้า (No pre-computation) แต่ในแต่ละก้าว หนูจะตรวจสอบกำแพงรอบตัวตามทิศทางที่หันหน้าอยู่ และตัดสินใจเดินตามลำดับความสำคัญต่อไปนี้:
  1. ถ้าไม่มีกำแพงด้านขวา -> เลี้ยวขวาและเดินหน้า 1 ก้าว
  2. ถ้ามีกำแพงด้านขวา แต่ไม่มีกำแพงด้านหน้า -> เดินตรงต่อไป 1 ก้าว
  3. ถ้ามีกำแพงด้านขวาและด้านหน้า แต่ไม่มีกำแพงด้านซ้าย -> เลี้ยวซ้ายและเดินหน้า 1 ก้าว
  4. ถ้ามีกำแพงล้อมรอบทั้งสามด้าน (ทางตัน) -> กลับตัว (หันหลัง) และเดินหน้า 1 ก้าว
- **เงื่อนไขเวลา**: ตัวโปรแกรมถูกเขียนขึ้นมาให้ติดตามเวลาตามจริง (Real wall-clock time) โดยหนูมีเวลาตัดสินใจ 3 นาที หากครบเวลา 180 วินาที ระบบจะตัดจบและระบุว่า "DEAD"

## 3. Prompt ที่ใช้สื่อสารกับ AI
สามารถคัดลอก Prompt นี้ส่งให้อาจารย์ดูได้:

> **Prompt:** "ลองอ่านไฟล์ในนี้ที่เราทิ้งไว้ให้แล้ว เป็นการบ้านจากอาจารย์ แกลองอ่านแล้วว่าไงลองวางแผนมาให้ดูหน่อย 
> ช่วยเขียนโปรแกรมจำลองเขาวงกตด้วย Python ขนาด 30x30 ช่อง โดยใช้ PySide6 เพื่อสร้าง GUI เป็นมุมมอง 2D Top-down 
> เขาวงกตต้องมีจุดเริ่มต้นและทางออกอย่างละ 1 จุด เส้นทางเดินต้องให้ตัวแทนหนู (Mouse agent) มีการตัดสินใจ (เลี้ยว/แยก) ไม่น้อยกว่า 5 ครั้ง 
> และให้ตัวหนูใช้การเดินด้วยอัลกอริทึมแบบไม่รู้แผนที่ล่วงหน้า (Local Decision - กฎเดินชิดขวา หรือ Right-Hand Rule) เพื่อหาทางออก 
> ภายในเวลาจำกัด 3 นาที (180 วินาที) หากเกินเวลาให้หนูตาย 
> พร้อมทั้งวาดเส้นประเชื่อมโยงแสดงทิศทางตรงจากหนูไปหาเป้าหมายตลอดเวลา"

## 4. การบันทึกวิดีโอ (สำหรับส่งวันจันทร์หน้า)
- ให้เปิดโปรแกรม แล้วกดปุ่ม **Start** (สามารถปรับ Slider เพิ่มความเร็วจำลองได้เพื่อให้วิ่งเร็วขึ้น)
- ใช้โปรแกรมบันทึกหน้าจอ (เช่น OBS หรือ Windows Game Bar) บันทึกตั้งแต่หนูเริ่มวิ่ง จนกระทั่งหนูถึงชีสและระบบเปลี่ยนสถานะเป็น SUCCESS
